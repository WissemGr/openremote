{
  "name": "openremote",
  "packageManager": "yarn@4.1.0",
  "private": true,
  "workspaces": [
    "ui/util",
    "ui/app/*",
    "ui/component/*",
    "ui/demo/*"
  ],
  "scripts": {
    "build": "./build-vercel.sh",
    "build:manager": "cd ui/app/manager && yarn run modelBuild && yarn run build"
  },
  "devDependencies": {
    "@openremote/util": "workspace:*"
  }
}
